class Constants {
  static const double MAX_VIEW_WIDTH = 400.0; // i.e. the maximum width a layout (especially form) should contain to avoid awkward stretching of widgets on larger screens
  static const double COLUMN_SPACING = 16.0; // i.e the space between 2 widgets stacked vertically
  static const double LAYOUT_PADDING = 16.0; //i.e the padding for a layout
  static const double LAYOUT_BOTTOM_PADDING = 8.0; //i.e. only applies to layout bottom padding
  static const String APP_NAME = "Book-Mob";
}